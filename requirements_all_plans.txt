# Requirements cho tất cả 5 phương án pub/sub

# Core dependencies (chung cho tất cả)
paho-mqtt>=1.6.0
requests>=2.25.0
psutil>=5.8.0

# Phương án 1: MQTT + VictoriaMetrics
# paho-mqtt (đã có ở trên)
# requests (đã có ở trên)

# Phương án 2: MQTT + File Storage  
# paho-mqtt (đã có ở trên)
# csv, json, os (built-in Python)

# Phương án 3: Apache Pulsar + Python
pulsar-client>=3.0.0

# Phương án 4: NATS + Python
nats-py>=1.0.0

# Phương án 5: RabbitMQ + Python
pika>=1.3.0

# Optional: Monitoring và analysis
matplotlib>=3.5.0
pandas>=1.3.0
numpy>=1.21.0
